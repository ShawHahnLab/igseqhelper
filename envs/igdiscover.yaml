# conda environment YAML for IgDiscover (bioconda+github fork)
name: igdiscover
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - igdiscover>=0.15,<0.16
  # for pkg_resources
  # https://stackoverflow.com/a/79886564
  - setuptools<82
